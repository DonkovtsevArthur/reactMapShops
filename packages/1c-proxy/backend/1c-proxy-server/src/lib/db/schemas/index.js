const user = require('./user');
const NewEmailRequest = require('./newEmailRequest');
const resetPassword = require('./resetPassword');

module.exports = {
  user,
  NewEmailRequest,
  resetPassword,
};
