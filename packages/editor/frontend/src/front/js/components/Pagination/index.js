import React from 'react'
import PropTypes from 'prop-types'

const Pagination = props => {
	return (
		<div>
			<strong>{console.log(props)}This is pagination!</strong>
		</div>
	)
}

Pagination.propTypes = {}

export default Pagination
